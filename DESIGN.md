# Product design identity — horse-talent

디자인 의도의 단일 명세다. 값은 `src/ui/tokens.ts`, 여기는 의미와 사용 원칙.

## Purpose

한 판 **10~20분**짜리 1인 경마 게임을 캔버스 한 장에 그린다.
아트는 메이플스토리 계열의 **귀여운 캐릭터성**이지만, 이 게임의 판단 재료는
**숫자**(배당·확률·골드·능력치)다. 그래서 원칙 하나가 다른 모든 것을 이긴다 —

> **귀여움이 숫자를 가리지 않는다.**

캐릭터는 감정을 만들고 숫자는 결정을 만든다. 둘이 같은 화면에서 싸우므로
**층을 나눈다**: 캐릭터는 트랙·마방 층, 숫자는 UI 층.

## Design principles

1. **귀여움이 숫자를 가리지 않는다.** 반짝임·모션은 판정 숫자 위에 겹치지 않는다.
   장식을 넣기 전에 그 자리에 판단 재료가 있는지 본다.
2. **내 말은 화면에서 유일하게 채도가 높다.** 6두가 동시에 달리므로
   내 말을 0.1초에 찾을 수 있어야 한다. 상대는 `shade(…, 0.82)` 로 눌러 그린다.
3. **상태를 색 하나로만 구분하지 않는다.** 말은 색 + **번호포 숫자** + 이름을 같이 준다.
   강화 세 갈래는 색 + **글자**(성공/보존/감소) + 막대 폭을 같이 바꾼다.
4. **숫자 옆에 항상 모수를 적는다.** `6.4배` 가 아니라 `적중 시 4,096 G (6.4배)`,
   `45%` 가 아니라 `성공 48% · 보존 33% · 감소 19%` 세 개를 다 적는다.
5. **확률을 숨기지 않는다.** 사용자가 명시 요청했고, 숨기면 이 게임의 판단 재료가 사라진다.
6. **새 색이 필요하면 먼저 여기에 역할을 적는다.** 씬에 리터럴을 쓰지 않는다.
7. **표정은 내 말에만.** 상대까지 표정을 주면 시선이 분산되고,
   감정을 이입할 대상은 하나뿐이다.

## 화면 규격 — 960 × 640 (HT-001 에서 확정)

nan 의 640×480 을 물려받지 않았다. 그 값은 nan 의 전제("시연용 한 화면 · 판독성 최우선")
에서 나온 것이고, 이 게임은 **배당표 6행 + 확률 3종 + 능력치 3종**을 동시에 띄운다.

| 영역 | y |
|---|---|
| 헤더 (라운드 · 거리 · 말 이름 · 골드) | 0 ~ 58 |
| 본문 | 78 ~ 600 |
| 안내줄 | 618 |

좌표는 `scenes/game.ts` 의 `L` 에 있고 **`src/layout.test.ts` 가 겹침을 검사한다.**
손으로 고른 좌표를 눈으로만 확인하지 않는다 — nan 이 정적 화면에서만 겹침을 여섯 번 냈다.

## Color palette

역할 기반. 실값은 `src/ui/tokens.ts`.

**2차 아트 방향 (HT-003).** 1차의 나무 패널 + 크림색 버튼은 낡아 보였다.
지금은 요즘 웹 도구(media.io 계열)의 문법이다 —
**깊은 남보라 바탕 · 유리 카드 · 그라데이션은 주 동작 하나에만.**

| 역할 | 토큰 | 용도 |
|---|---|---|
| 바탕 | `C.bg0` / `C.bg1` | 위아래 옅은 그라데이션. 납작한 단색보다 깊이가 산다 |
| 유리 카드 | `C.card` / `C.line` | **항상 `withAlpha` 와 함께.** 면 4~7% · 테두리 8~16% |
| 주 동작 | `C.accent0` → `C.accent1` | 보라 → 청록 그라데이션. **화면에 하나만** |
| 글자 | `C.text` / `C.textMuted` / `C.textFaint` | 본문 · 보조 · 부가 |
| 밝은 면 위 글자 | `C.ink` | 그라데이션 버튼·금색 배지 위 |
| 화폐 | `C.gold` / `C.goldDk` | 골드, **내 말 강조** |
| 강화 성공/보존/감소 | `C.success` / `C.keep` / `C.drop` | 세 갈래. 색 + 밝기 + 글자 + 막대 폭 |
| 트랙 | `C.turf` / `C.turfAlt` / `C.stand` | 레인 교대색, 관중석 |
| 순백 | `C.white` | 번호포·눈 흰자·결승선 **여기서만** |
| 눈물 · 말굽 | `C.tear` / `C.hoof` | 치비 디테일 |

**`MINE_COAT` 는 `C.gold` 와 같은 값이다** — 이 게임에서 내 말은 곧 내 자산이라 의도한 것이다.

`RIVAL_COATS` 6종은 서로 색상환에서 갈라 두었고, 그래도 **번호포 숫자가 최종 판독 수단**이다(원칙 3).

### 카드와 버튼

`card()` 가 유리 카드 한 장을 그린다 — 요즘 UI 의 기본 단위다.
`accentGrad()` 는 주 동작 그라데이션이고 **한 화면에 하나만** 쓴다.
여러 개면 무엇이 주인지가 사라진다.

버튼은 셋뿐이다: **기본**(유리) · **선택됨**(옅은 강조 + 밝은 글자) · **주 동작**(그라데이션).

## Interaction states

캔버스 게임이라 DOM 상태가 없다. 대응하는 상태를 다음처럼 그린다.

| 상태 | 어떻게 보이나 |
|---|---|
| **default** | 유리 카드 — 면 6% · 테두리 12% · 글자 `C.textMuted` |
| **selected / active** | 면 16% · 테두리 34% · 글자 `C.text`. 승식·금액·선택한 말 행 |
| **primary** | `accentGrad()` 채움 + 흰 글자. **한 화면에 하나** (걸고 출발 · 창구로 · 다음 경주) |
| **disabled** | 면 2% · 테두리 5% · 글자 `C.textFaint`. **클릭 판정에서 빠진다** — 눌리는데 반응이 없는 상태를 만들지 않는다 |
| **hover** | **승식 버튼에만.** 고르지 않고도 그 승식 설명을 미리 보는 수단이다. 판단을 바꾸지는 않는다 |
| **focus** | 이름 입력에서 **깜빡이는 캐럿**으로 대신한다 |
| **help** | 없다. **도움말 버튼을 두지 않는다** — 눌러야 보이는 설명은 안 눌러본 사람에게 없는 것과 같다. 승식 설명은 창구에 **항상** 떠 있다 |
| **loading** | 없다. 전부 로컬 계산이라 대기가 없다 |
| **empty** | 베팅 안 함 = `안 걸고 출발` 버튼이 항상 열려 있다 |
| **error** | 골드 부족은 에러 메시지가 아니라 **버튼 disabled** 로 미리 막는다 |
| **success** | 적중은 `C.success`, 꽝은 `C.drop`, 환급은 `C.textMuted` 로 정산 문구가 갈린다 |

## Motion

- **강화 판정 3단** — 돌린다 0.8s → 감속 0.55s → 판정 0.3s. **총 1.65s 로 묶는다.**
  긴장을 만들려던 시간이 기다림이 되면 안 된다(nan PSH-005 에서 배운 것).
  **결과는 먼저 확정하고 화면만 늦춘다** — 연출 중 판정은 결정성과 충돌한다.
- **경주 — 1착이 5초에 결승선을 끊고, 3착이 들어올 때까지 더 돈다.**
  승식이 3착까지 보는데 1착에서 끊으면 판정을 못 본다. 들어온 말에는 착순 배지가 붙는다.
  언제든 스킵 가능 — 10~20분 게임에서 안 넘길 수 있는 연출은 죄다.
- **달리기** — 몸이 통통 뛰고 갈기·꼬리가 늦게 따라온다(`lag`). 캐릭터성의 대부분이 여기서 나온다.
- 반짝임은 **강화 성공에만.** 남발하면 원칙 1을 깬다.

## Voice

숫자는 그대로, 문장은 짧게. 결과를 사람 말로 적는다 —
`보존 — 능력치 그대로`, `관중은 이 결과를 모른다`.
퍼센트로 감정을 설명하지 않는다.

## Do

- 확률 세 개를 항상 같이 보여준다.
- 승식 설명을 **항상** 띄운다. 버튼 뒤에 숨기지 않는다.
- 그라데이션은 **주 동작 하나**에만.
- 배당 옆에 적중 시 금액을 같이 적는다.
- 내 말에는 이름을, 모든 말에는 번호를.

## Do not

- 강화 확률을 숨기거나 "대략" 표기하지 않는다.
- 상대 말에 표정을 주지 않는다.
- 파티클을 숫자 위에 얹지 않는다.
- 새 색을 씬에 리터럴로 쓰지 않는다.
- 도움말을 버튼 뒤에 숨기지 않는다.
- 그라데이션을 장식으로 쓰지 않는다.
