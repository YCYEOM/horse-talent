# DECISIONS — horse-talent

날짜 · 결정 · 왜 · 어디. **왜**가 이 표의 값이다 — 무엇을 했는지는 git 이 안다.

| 날짜 | 결정 | 왜 | 파일 |
|---|---|---|---|
| 08-06 | nan 과 **별도 프로젝트**로 시작한다 | 한 판 10~20분은 nan 의 게이트(6시간 세로 조각 · 한 문장 루프 · 30초 판)와 정면으로 싸운다. 같은 저장소에 두면 게이트를 느슨하게 만들거나 게임을 짧게 만들거나 둘 중 하나가 된다 | — |
| 08-06 | BASS 하네스는 **그대로**, 게이트만 다시 쓴다 | 컨셉 승인·trace·evidence·run record·세션 보호는 판 길이와 무관하게 값이 있다. 짧은 게임 전용인 것은 **하드게이트의 뜻**과 48시간 잼 시계뿐이다 | bass.yaml, nan/gates.yaml |
| 08-06 | 하드게이트 7개의 **이름은 유지, 뜻은 재정의** | 플랫폼 컨셉 스키마가 `.strict()` 라 키를 못 늘린다. 이름을 바꾸면 `bass nan concept gate` 가 깨진다. 그래서 `nan/gates.yaml` 의 `definitions` 에 이 프로젝트 기준 해석을 적었다 — 특히 `vertical-slice-in-six-hours` 를 **"첫 3분"이 6시간**으로 | nan/gates.yaml |
| 08-06 | `projectGates` 5개 신설 — 긴 판에서만 생기는 실패를 막는다 | 곡선 · 결정의 구속력 · 길이의 출처 · 늘어지는 구간 · 실측 길이. **nan 에는 하나도 없었다** — 30초짜리에 곡선이 필요 없었기 때문이다 | nan/gates.yaml |
| 08-06 | 채점 축에 **`arcQuality` 20점** 추가 | 이 프로젝트의 1번 위험이 "후반이 초반의 반복"이라 게이트와 채점 양쪽에서 같은 것을 묻는다 | nan2026.yaml |
| 08-06 | 잼 시계(T+6~T+42)를 **마일스톤 M1~M5** 로 | 대회가 아니다. 시계가 아니라 산출물이 게이트다 — 첫 3분 → 완주 → 곡선 → 동결 → 출하 | nan/gates.yaml, nan2026.yaml |
| 08-06 | 툴체인은 nan 과 동일(Vite + TS + vitest). **화면 규격은 미정** | 하네스·kits·토큰을 나중에 옮겨 쓸 수 있다. 다만 640×480 은 nan 의 전제(시연용 한 화면)이지 이 게임의 전제가 아니라 **컨셉 승인 뒤에 정한다** | package.json, DESIGN.md |
| 08-06 | ⚠ (버그) **경주가 시작 전에 정해져 있었다.** 구간 잡음이 총 주행량의 3% 뿐이었다 | 플레이 지적 "승패는 시작전에 정해지는거같은데". 재보니 맞았다 — **인기마 승률 84% · 3착 이내 99.6% · 최약체 0.0%**, 내 말은 스탯 3 에서 0% 였다가 5 에서 90% 로 절벽처럼 뛰었다. 그러면 베팅이 예측이 아니라 산수가 되고 강화는 "이기는 말 만들기" 한 줄이 된다. **확률이 정하고 능력치가 그 확률을 미는** 구조로 바꿨다 | src/systems/race.ts |
| 08-06 | 무작위를 **두 겹**으로 — 경주당 한 번 뽑는 `CONDITION`(±42%) + 주행량에 **비례하는** `JITTER`(±40%) | 컨디션을 구간마다 뽑으면 10번 평균되어 사라진다 — **이변의 출처는 경주당 한 번짜리 값이어야 한다.** 잡음을 고정값으로 두면 능력치가 커질수록 상대적으로 작아져 후반에 다시 결정론이 된다. 결과: 능력치 1~6위 승률 **34/25/20/13/9/0** | src/systems/race.ts |
| 08-06 | ⚠ (버그) **단승 인기마 수익률이 +54% 였다** — 돈 찍는 기계 | 위 버그를 고치다 발견했다. 관중이 판돈을 **실력에 비례**해서 거는데 승률은 실력에 **초비례**한다(실력비 1.82배 → 승률비 6.5배). 인기마가 판돈의 20% 를 먹고 39% 를 이기니 구조적으로 +EV 였다. 공제율 20% 짜리 게임에서 어느 승식이든 +EV 면 경제가 통째로 무너진다 | src/systems/race.ts |
| 08-06 | 관중이 **실력을 승률로 옮겨서** 건다 — `CROWD_EXPONENT` (단승 6.0 · 연승 3.0) | 실제 패리뮤추얼에서 인기마가 공제율만큼만 손해인 이유가 이것이다 — **대중은 자기가 볼 수 있는 것의 가격을 잘 매긴다.** 승식마다 확률 분포가 다르므로 지수도 갈랐다. 결과: 단승 −8/−7/−8/−22/−34/−100%, 연승 −23/−19/−17/−21/−21/−95% — 전부 손해이고 **인기마가 대박마보다 덜 손해**다(실제 경마의 favorite-longshot bias 와 같은 모양) | src/systems/race.ts |
| 08-06 | **이 게임의 이득은 관중이 못 보는 것에서만 나온다** — 설계 의도가 수치로 확인됐다 | 공개 정보로 걸면 −4~−23%(공제율), **숨긴 강화로 걸면 +274%**(5R 스탯 8, 관중은 4.8 로 본다). 컨셉이 말한 "내가 아는 것과 배당이 아는 것의 차이"가 경제 지표로 재현된다 | src/balance.test.ts |
| 08-06 | 출주표 폭 `SPREAD` 1.5 → **1.1** | 넓으면 최약체가 **이길 수 없는 말**이 되어 그 마권이 순수 함정이 된다. 실제 경마도 비슷한 등급끼리 붙인다. 다만 최약체 승률은 여전히 0% 다 — **미해결로 남긴다** | src/systems/race.ts |
| 08-06 | (테스트) **분포를 검사하는 `balance.test.ts` 를 신설**한다 | **이 버그가 검사 43개를 전부 통과한 채로 존재했다.** 각 함수가 동작하는지만 봤지 어느 결과가 실제로 얼마나 나오는지는 아무도 안 봤다 — 인기마 승률을 84% → 34% 로 바꿔도 기존 검사는 하나도 안 깨졌다. **nan 이 두 번 겪은 병의 세 번째 사례다**(DEC-DGE-METRIC-WRONG · 딜레마 회귀). 새 검사 13개를 옛 상태에 걸어 5개가 빨개지는 것을 확인했다 | src/balance.test.ts |
| 08-06 | **승식 7종** 구현 (HT-002) — 연승·단승·복승·쌍승·복연승·삼복승·삼쌍승 | 사용자 요청. CON-001 이 첫 조각을 2종으로 못 박았고 M1 이 끝났으니 이제 편다. "승식이 곧 국면"이라는 컨셉의 곡선이 이제야 코드에 생긴다 | src/systems/race.ts |
| 08-06 | 배당 모델을 **몬테카를로 + Harville** 로 다시 쓴다. `CROWD_EXPONENT` 삭제 | M1 의 "판돈 ∝ 실력^6.0" 은 단승 EV 를 억지로 0 근처로 맞춘 **땜질**이었고 조합 승식에는 대응하는 지수가 없다. 이제 **관중이 자기가 아는 것으로 경주를 400번 상상**하고, 그 승률에서 Harville 로 순열 확률을 만든 뒤, **순열 하나에서 7종을 전부 유도**한다. 승식마다 따로 계산하면 복승 합 ≠ 쌍승 합 같은 모순이 생긴다. 수학적으로 EV = 1 − 공제율이 유도되고, 실측도 7종 전부 음수(−12~−67%)다 | src/systems/race.ts |
| 08-06 | `slots` — 승식별 **적중 선택지 수**를 스펙에 박는다 (연승 3 · 복연승 3 · 나머지 1) | 풀을 이 수로 나눠야 EV 가 −공제율로 떨어진다. 복연승이 3인 이유는 3착 안에서 두 마리 고르기라 C(3,2)=3 이기 때문이다. 검사가 `winningKeys` 길이와 스펙을 대조한다 | src/systems/race.ts |
| 08-06 | ⚠ (발견) **조합 승식에서는 숨긴 정보의 우위가 희석된다** | 5R 스탯 8 을 관중이 4.8 로 볼 때 — 단승 +275% · 쌍승 +107% · 복승 +69% 인데 **삼복승 −6% · 삼쌍승 −12%** 다. 내 말 말고 다른 말들의 착순도 맞혀야 하는데 거기엔 정보 우위가 없다. **CON-001 이 "후반엔 삼쌍승으로 크게 번다"고 적었는데 실측은 그렇게 단순하지 않다.** 우위 자체는 모든 승식에 있고(검사가 못 박음) 절대 수익이 양수인 것만 단순 승식이다 — M3 곡선 설계의 숙제로 남긴다 | src/balance.test.ts |
| 08-06 | 순서 승식은 **누른 순서가 곧 착순** | 규칙을 따로 만들지 않는다. 배당표 행에 `1착`·`2착` 배지가 찍혀 무엇을 고른 상태인지가 보인다. 이미 고른 말을 다시 누르면 빠지고, 정원을 넘기면 가장 먼저 고른 것이 밀려난다 | src/scenes/game.ts |
| 08-06 | 도움말을 **세 겹**으로 — 항상 보이는 한 줄 · 호버 미리보기 · `?` 오버레이 | 사용자 요청("설명을 써주던가 호버하면 보이던가 도움말처럼 빼놓던가"). 셋이 역할이 다르다: 한 줄은 기본, 호버는 **고르지 않고 탐색**, 오버레이는 학습이다. 오버레이는 **착순 4→2→6 하나로 7종을 전부** 설명한다 — 승식마다 다른 예를 들면 무엇이 다른지가 안 보인다 | src/scenes/game.ts |
| 08-06 | (테스트) 배치 검사가 창구 겹침 **3건을 잡았다** | 승식 그리드 4행이 설명 줄과, 설명 줄이 마권과, 출발 버튼이 안내줄과 겹쳤다. 눈으로 봤으면 넘어갔을 것이다 — nan 이 정적 화면에서만 겹침을 여섯 번 낸 이유가 이것이다. 좌표를 다시 잡고 출발 버튼 둘을 나란히 놓았다 | src/layout.test.ts |
| 08-06 | ⚠ (기능 결함) **경주가 1착에서 끊겨 3착을 못 봤다** | 플레이 지적 "승식에 연관이있는 3등까지는 들어오는걸 보여줘야하지않을까". 맞다 — 승식 7종 중 넷이 3착까지 보는데 화면은 1착이 선을 끊는 순간 끝났다. **판정 근거를 안 보여주는 화면**이었다. 경주를 연장 구간(EXTRA 10)까지 돌려 각 말이 선을 끊는 시각을 남기고, 3착이 들어온 뒤 0.8초에 끝난다. 들어온 말에는 착순 배지가 붙는다 | src/systems/race.ts, src/scenes/game.ts |
| 08-06 | 관중 시뮬에 `quick` 모드 — 연장 구간을 안 돈다 | 배당 한 번 뽑는 데 400판을 돌리는데 거기까지 연장하면 검사가 몇 배 느려진다. **관중은 1착만 알면 된다** | src/systems/race.ts |
| 08-06 | **도움말 버튼을 없앤다.** 승식 설명은 창구에 항상 떠 있다 | 플레이 지적 "아래에 이미 설명이 나와서 도움말 버튼이 굳이 필요없을거같아". 맞다 — **눌러야 보이는 설명은 안 눌러본 사람에게 없는 것과 같다.** 대신 항상 보이는 설명 카드에 예시까지 넣었다(`예) 4→2→6 이면 4,2 또는 2,4 적중`). 호버 미리보기는 남긴다 — 그건 탐색 수단이지 설명 은닉이 아니다 | src/scenes/game.ts, DESIGN.md |
| 08-06 | 아트 방향 2차 — **나무 패널 → 유리 카드 + 그라데이션** | 플레이 지적 "전체적인 디자인이 너무 구려", 참조로 media.io. 1차의 크림색 버튼 + 3px 갈색 외곽선은 낡아 보였다. 깊은 남보라 바탕 · 낮은 알파 유리 카드 · 큰 라운드 · 얇은 구분선으로 바꾸고, **보라→청록 그라데이션은 주 동작 하나에만** 쓴다. `card()`·`accentGrad()` 를 토큰에 넣어 씬이 색을 조립하지 않게 했다 | src/ui/tokens.ts, DESIGN.md |
| 08-06 | **캐릭터는 안 바꾼다** — 치비 말 그대로 | 낡아 보인 것은 UI 껍데기였지 말이 아니었다. 오히려 **차분한 UI 와 귀여운 말이 대비되어 말이 더 눈에 든다** — 사용자가 요청한 캐릭터성이 UI 를 정리하니 살아난다 | src/ui/horse.ts |
| 08-06 | ⚠ (버그) **강화 결과가 연출 전에 새어나왔다** — 화면이 옛 단계를 보여주게 고침 (`revealLevel`) | 플레이 지적 "막대기가 움직이고 마지막에 성공인지 실패인지 나오는데 그게 끝나기전에 스탯이 먼저 반영되서 먼저 알수 있어". 맞다 — `forge()` 가 누르는 순간 엔진 상태를 바꾸는데(결정성 때문에 그래야 한다) 화면이 그 값을 그대로 읽고 있었다. **바늘이 돌기도 전에 답이 보이니 1.65초 연출이 통째로 무의미**했다. nan PSH-004 의 "결과를 먼저 확정하고 화면만 늦춘다"를 가져오면서 **늦춰야 할 화면이 바늘만이 아니라는 것을 놓쳤다** — 단계 숫자·막대·강화 비용·확률 표시가 전부 답을 말하고 있었다. `shownLv()` 가 판정 시각(1.35s) 전까지 옛 값을 돌려준다 | src/scenes/game.ts |
| 08-06 | (테스트) 판정 시각을 `REVEAL` 상수로 빼고 **바늘과 숫자가 같은 시각에 드러나는지**를 검사한다 | 둘이 어긋나면 또 샌다. `revealLevel()` 을 순수 함수로 빼서 검사 가능하게 만들었다 — 렌더 안에 묻어두면 이런 종류는 영영 검사가 안 된다 | src/layout.test.ts |
| 08-06 | **M2 시작** — 경주 **8~12회 랜덤**. 남은 수는 화면에 **범위로만** 알린다 | 정확히 알면 마지막 경주에 전 재산을 걸면 되고, 그러면 후반 판단이 하나로 수렴한다. 판마다 다른 것이 "다음 판에 회수하지"를 막는다 | src/systems/session.ts |
| 08-06 | ⚠⚠ **이 게임에는 수입원이 없었다.** 정책 넷을 12경주 × 400판 돌렸더니 **넷 다 졌다** | M1 의 검사는 경주 **하나씩**만 봐서 못 봤다. 12경주로 늘리자 드러났다 — 베팅은 공제율 때문에 **모든 승식이 −EV** 이고 강화는 골드를 태우기만 한다. **안 거는 전략조차 0.19배**로 끝났다(인기마 연승 0.05배, 항상 내 말 0.00배·파산 98%). 값이 아니라 **구조의 구멍**이었다 | src/session.test.ts |
| 08-06 | **상금 신설** — 내 말이 3착 안에 들면 받는다 (1·2·3착 60/22/12%, 경주마다 커진다) | 빠진 것은 실제 경마에 있었다 — **마주는 베팅이 아니라 상금으로 번다.** 넣자 강화가 판당 4~6회에서 8회로, 참여율이 98% 가 됐다. 두 수입원의 성격이 다른 것이 오히려 이 게임에 좋다: **상금은 강화의 직접 보상**(확실하지만 작다), **베팅은 정보 우위의 레버리지**(위험하지만 크다). **CON-001 은 "순위를 맞추면 골드"만 적었다 — 컨셉이 수입원을 정하지 않은 구멍이었다** | src/systems/session.ts |
| 08-06 | (지표 수정) "최종 골드 0.8~2.5배"를 버리고 **"끝까지 선택할 수 있었는가"** 로 | 처음 목표는 골드를 **점수**로 본 것이었다. 골드는 점수가 아니라 **강화 연료**이고, 한 자리에서 끝나는 게임은 연료를 다 쓰고 끝나는 게 맞다. 참여율(베팅 가능했던 경주 비율) > 90% · 파산 3~35% · 최종 골드 < 3배(폭발 방지)로 바꿨다. **결과에 맞춰 목표를 고친 게 아니라 목표가 무엇을 재는지가 틀렸다** | src/session.test.ts |
| 08-06 | 상금·시작 골드를 **실측으로 유도**한다 (상금 3600 + 경주×420, 시작 1600) | 상금 규모 6종을 400판씩 훑어 강화 8회 · 참여율 98% · 파산 12% 가 나오는 값을 골랐다. M1 의 시작 골드 1,000 은 2경주 기준이라 틀렸다 | src/systems/session.ts |
| 08-06 | 파산해도 **판은 끝까지 간다** | 중간에 끊으면 "얼마나 버텼나"가 안 남는다. 골드가 최소 베팅액 미만이면 `걸고 출발` 이 비활성이 되고 `안 걸고 출발` 이 주 동작이 된다 — 그때부터는 구경이고, 결산이 **몇 경주째에 말랐는지**를 적는다 | src/scenes/game.ts |
| 08-06 | 결산을 12경주용으로 — **최고 배당 한 건을 크게** + 요약 5줄 | 2경주 기준 로그 나열은 12경주에서 넘친다. 페이오프가 숫자가 아니라 **사건**이 되게 최고 적중을 카드로 띄우고, 나머지는 강화 이력·베팅 적중률·상금·파산 시점 요약으로 접었다 | src/scenes/game.ts |
| 08-06 | **M3 — 곡선은 이미 있었다.** 안 재본 수가 둘 있었다 | 처음 측정에서 중반·후반 최선의 수가 둘 다 "단승"이라 `curve-not-repetition` 에 걸리는 줄 알았다. 그런데 **역베팅과 후반 상금 배분을 한 번도 안 쟀다.** 재보니 국면마다 최선의 수가 다른 범주였다 — 곡선이 없는 게 아니라 **내 측정이 좁았다.** 값을 안 만지고 화면만 고쳤다 | src/curve.test.ts |
| 08-06 | (발견) **역베팅** — 관중이 내 말을 과대평가하면 인기마가 싸진다 | 강화가 감소하면 내 말은 실제보다 약한데 관중은 모른다. 그만큼 인기마가 저평가되어 **인기마 단승이 +45%** 가 된다(실제 5 / 평판 8). 평판이 정확하면 −29% 라 **우위가 전적으로 오가격에서 나온다.** 컨셉이 몰랐던 수이고, 정보 비대칭이 **양방향**이라는 뜻이라 컨셉을 오히려 강화한다 | src/curve.test.ts |
| 08-06 | (발견) **후반은 베팅이 아니라 강화 배분이 수입을 정한다** | 후반 한 경주 상금 2,384 G 가 최선 베팅 우위(+29% = 100G 걸어 29G)를 압도한다. 같은 총 스탯 27 인데 배분에 따라 상금이 **382~3,939 G** 로 갈린다(장거리형이 1000m 에서 382, 2000m 에서 3,939). **판단이 창구에서 마방으로 옮겨간다** — 종류가 다른 결정이다 | src/curve.test.ts |
| 08-06 | **평판 대비 실력을 화면에 띄운다** — `관중 5.0 / 실제 7.2 · 저평가` | **이 게임의 유일한 우위 원천이 화면 어디에도 없었다.** 판단 재료 없이 판단하라고 요구한 셈이고 DESIGN.md 원칙 5 위반이었다. 저평가/제값/과대평가 셋으로 갈라 색과 글자를 같이 바꾼다(원칙 3). 창구에 이번 경주 상금도 같이 띄운다 — 후반에 상금이 커지는 것이 보여야 "베팅보다 완주"가 판단이 된다 | src/scenes/game.ts |
| 08-06 | ⚠ **CON-001 을 고치고 재승인 대기로 돌린다** | 승인된 컨셉의 두 문장이 실측에 반박당했다 — (1) "후반엔 삼쌍승으로 크게 번다"(실측 −68%) (2) 수입원을 안 적었다(상금 누락). **조용히 코드만 맞추면 문서가 거짓말이 된다.** 반박 내용과 실측 곡선을 컨셉에 적고 2차 승인을 요청한다 | nan/concepts/CON-001.yaml |
| 08-06 | 상금 곡선을 **평평하게** — `3600 + 420×경주`(2.15배) → `5100 + 120×경주`(**1.25배**) | 사용자 지적 "후반은 상금 극대화보다 소폭 상승 정도로". 맞다 — 상금이 후반에만 크면 **초반이 버리는 구간**이 되고 후반 판단이 하나로 수렴한다. 총 수입은 유지하고(기본을 올려 맞춤) 분포만 폈다. **뜻밖의 소득**: 국면 차이가 이제 상금 크기가 아니라 **내 말이 실제로 3착 안에 드는가**에서 나온다 — 경주당 상금은 1.25배 오르는데 실제 수입은 373 → 1,862 G 로 5배 오른다. 그쪽이 더 정직한 곡선이다. 경제 지표(강화 8 · 참여율 98% · 파산 11%)는 어느 곡선에서도 같았다 | src/systems/session.ts |
| 08-06 | (테스트) "후반 경주가 1.5배 이상 크다" → **"오르되 1.5배 미만"** 으로 뒤집는다 | 옛 기대값이 새 의도를 막았다. 검사가 정확히 잡았고, 값이 아니라 **의도**(소폭 상승)를 담게 고쳤다 | src/session.test.ts |
| 08-06 | **대상경주(G1) 신설** — 마지막 1회 · 상금 3배 · **거리를 판 시작에 공개** | 사용자 지정. 핵심은 3배가 아니라 **거리 사전 공개**다 — 지금까지는 매 경주 랜덤 거리에 반응만 했는데, 목표를 알면 **8경주 동안 그 거리에 맞춰 쌓는 게 계획**이 된다. M3 가 찾은 구멍 셋(초반이 죽었다 · 후반 판단이 하나 · 특화가 도박이다)을 한 번에 막는다. **거리만** 공개한다 — 출주표까지 열면 정보 우위가 흐려진다 | src/systems/session.ts |
| 08-06 | **능력치 3 → 5종.** 넷째·다섯째는 **축이 다르다** | 사용자 요청. 속력·가속·체력은 전부 "어느 거리에 강한가" **한 축**이라, 넷째를 또 거리 스탯으로 넣으면 선택지만 늘고 판단은 안 는다. **근성**(경합 축 — 막판에 선두와 가까울 때만 값을 한다. 체력은 혼자 안 무너지는 것이고 근성은 붙어 있을 때 안 밀리는 것) · **안정**(기복 축 — 컨디션 폭을 줄인다. **국면에 따라 값이 뒤집히는 유일한 스탯**: 강할 때는 상금을 지키고 약할 때는 이변을 못 낸다) | src/systems/stable.ts, race.ts |
| 08-06 | ⚠ (연쇄) **상대 성장률 0.45 → 0.32.** 스탯이 늘자 죽음의 나선이 생겼다 | 0.45 는 **강화 8회를 스탯 3종에 몰아넣던 시절 값**이다. 5종이 되어 같은 예산을 더 나누게 되자 상대를 못 따라가고 → 입상 실패 → 상금 감소 → 강화 감소로 돌았다. **대상경주 입상이 한 판 상금의 3% 까지 떨어졌다.** 0.32 로 낮추니 강화 8회 · 참여율 99% · 파산 6% · 대상경주 몫 9% 로 섰다. **규모가 바뀌면 이전 값이 전부 의심 대상**이라는 M2 교훈이 그대로 재발했다 | src/systems/race.ts |
| 08-06 | (버그) **착순을 결승선 시점 위치 → 통과 시각**으로 | 근성이 연장 구간에서 순위를 바꾸자 착순(`order`)과 통과 순서(`finish`)가 어긋났다. **"먼저 들어온 말이 앞순위"가 착순의 정의**이므로 통과 시각으로 정한다. 검사가 잡았다 | src/systems/race.ts |
| 08-06 | ⚠ (테스트 버그) **`formOf` 가 0.45 를 하드코딩하고 있었다** | 상대 성장률을 0.32 로 바꿨는데 검사의 복사본이 안 따라와서, 관중이 내 말을 과대평가하는 상태가 되어 **"인기마 단승 +8%"** 라는 가짜 결과가 나왔다. 노이즈인 줄 알고 표본을 키우다가 발견했다. 이제 엔진에서 `RIVAL_GROWTH` 를 가져온다. **손으로 고른 상수로 데는 네 번째다**(TASK-014 · PSH-005 · PSH-006 계열) | src/balance.test.ts |
| 08-06 | (테스트) EV 검사가 **경주·배당을 승식끼리 재사용**한다 | 승식마다 새로 뽑으면 관중 몬테카를로를 7배 돌려 표본을 못 키운다. 그래서 n=700 이었고 조합 승식이 변동으로 +6% 처럼 보였다(n=3000 에서 −8%). 공유하니 **같은 시간에 표본 2500** 을 쓴다 | src/balance.test.ts |
| 08-06 | 강화 연출을 **가운데 오버레이**로 | 능력치가 5행이 되어 행 아래에 자리가 없다. 판정 순간은 화면을 독점해도 되는 유일한 순간이라 오히려 낫다 | src/scenes/game.ts |
| 08-06 | **M4 동결.** 시스템 2 · 승식 7 · 능력치 5 · 페이즈 6 · 경주 12 · 6두 · 거리 6종 | 이후 변경은 **값과 표시만**. 규칙이 바뀌면 M4 위반이고 새 마일스톤이다. **문서에만 적으면 다음 요청에 조용히 늘어나므로 검사로 만든다**(`freeze.test.ts`) — 깨지면 "실수로 늘렸거나 M4 를 열어야 하거나" 둘 중 하나이고, **검사를 고치는 것이 먼저가 아니다** | nan/gates.yaml, src/freeze.test.ts |
| 08-06 | 미해결 14건을 **셋으로 가른다** — 고친다 / 받아들인다 / 사람이 판정한다 | "나중에"라는 상태를 없앤다. **받아들인 넷**: 조합 승식 우위 희석(고치려면 새 시스템이 필요 — M4 위반) · 강화 확률표 값(분포는 검사가 지키고 값은 취향) · 공제율 20%(실제 경마 범위) · 상금이 원안에 없던 것(2차 승인으로 닫힘) | tasks/HT-007.md |
| 08-06 | ⚠ (버그) **근성이 안 돌고 있었다** — 셋이 틀렸다 | 효과 계수를 5배 올려도 결과가 안 변해서 발견했다. (1) `Math.max(...total)` 이 자기 자신을 포함해 **선두가 근성을 가장 크게 받았다** — 쫓는 힘이어야 하는데 도망가는 힘이었다. (2) 루프 안에서 `total` 을 고치는 중에 읽어 **게이트 순서에 결과가 달렸다.** (3) 경합폭 6% 가 누적거리로 **반 구간도 안 돼** 아무도 안 걸렸다. **HT-006 의 근성 검사는 통과한 채였다** — 3 대 10 을 비교해 "크다"만 봤지 얼마나 큰지는 안 봤다 | src/systems/race.ts |
| 08-06 | 근성 값 — 효과 0.09 → **0.7**, 경합폭 0.06 → **0.30** | 버그를 고쳐도 여전히 승률 +2%p(속력 +25%p)였다. **구조적으로 약할 수밖에 없다** — 거리 스탯은 10구간 전부에 걸리는데 근성은 마지막 3구간, 그것도 사정권일 때만이라 **노출이 1:7** 이다. 0.7 에서 거리 스탯의 **49%** 가치가 되고 인기마 승률은 32% 로 유지된다. 그 아래면 아무도 안 올리는 **가짜 선택**이다 | src/systems/race.ts |
| 08-06 | (판독성) 배당표에 **관중 예상 승률**을 적는다 | 최약체는 승률 0.2% · 3착 5% 인데 화면에는 "80.0배"라는 매력적인 숫자만 있었다 — **순수 함정**이다. 값으로 고치려면 출주표를 좁혀야 하고 그러면 다양성이 죽는다. **이미 화면에 있는 것(배당)을 읽히게** 만드는 쪽을 골랐다 — 새 정보가 아니라 사람이 암산 못 하는 것을 대신 해주는 것이다. 합이 1 인 것과 배당 순서와 역순인 것을 검사가 못 박는다 | src/scenes/game.ts |
| 08-06 | (판독성) 창구 상단 줄에서 **중복을 뺀다** | 헤더가 이미 대상경주와 거리를 말하는데 상단 줄이 상금을 또 말했다. 거리·상금·공제율·연승 기준을 한 줄로 합쳤다. **M4 의 개선 방향은 더하는 것이 아니라 빼는 것이다** | src/scenes/game.ts |
| 08-06 | **파산해도 판을 안 끊는다** — 대신 회복이 한 발 앞이라는 것을 화면이 말한다 | 사용자 질문 "파산했으면 그대로 끝내야하나, 내 말이 이길때까지 기다렸다 다시 시작해야하나". 재보니 **제 전제도 질문의 전제도 실제와 달랐다** — 파산은 판의 8% 에서 일어나고 **거의 항상 마지막 1~2경주**다(파산 시점 중앙 9R / 전체 10R). 89% 가 회복 못 하는 건 회복이 어려워서가 아니라 **남은 경주가 없어서**이고, 한 번 마르면 회복까지 걸리는 건 **중앙값 1경주**다. 즉 "기다린다"는 상황 자체가 거의 안 생긴다. 즉시 종료(B안)는 **8%의 판이 대상경주를 못 보고 끝난다** — 클라이맥스를 잘라내는 셈이라 기각 | src/scenes/game.ts |
| 08-06 | (판독성) 파산 화면을 **"이제 말만 믿는다"** 로 | 비활성 버튼만 남기면 "고장난 화면"으로 읽힌다. 마권 자리를 회복 안내로 바꾸고(`3착만 해도 +N G`), 주 동작을 `말만 믿고 출발`(대상경주면 `말만 믿고 대상경주로`)로 바꿨다. **파산이 실패가 아니라 이야기의 한 대목**이 되게 — 돈은 떨어졌고 남은 건 내 말뿐인 채로 대상경주에 들어간다. **규칙 변경 0, 표시만**(M4 범위) | src/scenes/game.ts |
| 08-06 | (검사) **회복이 구조적으로 참인지**를 못 박는다 | 문구로 "3착이면 회복된다"고 적어놓고 실제로 안 되면 거짓말이다. 어느 경주에서든 3착 상금 ≥ 최소 베팅 × 3 인 것과, 대상경주는 회복 폭이 더 큰 것을 검사한다 | src/freeze.test.ts |
| 08-06 | 승인 전 게임 코드 금지 | `nan2026.yaml` 이 concept 을 사람 승인 대상으로 건다. `src/main.ts` 는 진입점 자리만 잡았다 | src/main.ts |
| 08-06 | **경주 수 8~12 랜덤 → 12회 고정** (사용자 지정) | 사용자가 한 판을 실측해 **약 10분**이라고 판정하고 고정을 요청했다. **08-06 의 랜덤 결정을 뒤집는 것이다** — 그때 이유는 "정확히 알면 마지막 경주에 전 재산을 걸면 된다"였고, **그 방어가 사라진다.** 12R 이 대상경주(3배)인 것을 아니까 아껴뒀다 몰아넣는 것이 정답에 가까워진다. 남은 제동은 패리뮤추얼(크게 걸면 내 배당이 내려간다)뿐이다. 사용자가 실제 플레이 뒤 택했으므로 그대로 간다 | src/systems/session.ts |
| 08-06 | `honest-length` 게이트를 **닫았다** — 실측 10분 | 목표 10~20분의 아래 끝. 추정이 아니라 사람이 실제로 완주해서 잰 값이다. 다만 **그 10분은 12R 고정 이전**이고, 랜덤 구간 중앙이 10경주였으므로 지금은 12분 안팎으로 본다 — **이 추정치는 SPEC 에 숫자로 안 적는다** | nan/gates.yaml |
| 08-06 | **스탯 5종이 전부 결과를 움직인다는 것을 검사로 못 박았다** (사용자 질문) | "스탯이 실제로 경주에 반영되는가"에 답하려면 코드를 읽는 것보다 재는 것이 맞다. 하나만 3→10 으로 올려 1,200판 — 속력 +26%p · 체력 +21%p · 가속 +19%p · 근성 +13%p. **안정만 승률이 −1.8%p 인데 그게 설계다** — 컨디션 폭을 줄이면 요행 우승도 같이 깎이고, 대신 3착이 +4.4%p 오른다. 그래서 안정만 3착으로 검사한다 | src/balance.test.ts |
| 08-07 | **창구 겹침 3건 수정** — 설명 카드가 마권을 덮고 · `예상`+`승식` 머리글이 이어붙고 · 승식 예시가 카드 밖으로 넘쳤다 | 사용자가 화면에서 먼저 봤다. **검사가 셋 다 놓쳤다** — (1) 설명을 "한 줄 라벨 42px"로 모델링했는데 실제는 74px 카드였고, (2)(3) **세로만 검사하고 가로는 한 번도 안 쟀다.** 배치 검사를 여섯 번 데고 만들어놓고 축을 하나 빼먹었다 | src/scenes/game.ts, src/layout.test.ts |
| 08-07 | 카드 높이·열 x·예시 앞머리를 **코드와 검사가 같은 상수**에서 읽게 했다 (`L.poolDescH`·`L.colProb`·`EXAMPLE_ORDER`) | 74 와 590 을 양쪽에 손으로 적어둔 것이 이번 겹침의 직접 원인이다. **이 저장소가 손으로 고른 상수로 데는 다섯 번째** | src/scenes/game.ts |
| 08-07 | 배당표 `예상 승률` 열을 카드 **안**으로 옮겼다 (590 → 550, 단승 452→420 · 연승 534→490) | 밖에 있으니 표의 일부로 안 읽히고, x=590 이 승식 열 시작점과 같아 `2%` 가 복승 버튼에 붙었다 | src/scenes/game.ts |
| 08-07 | **말을 다시 그렸다** — 목·머리·갈기 셋을 고쳤다 | "말이 아닌 것 같다"는 지적. 부품이 없어서가 아니라 **말을 말로 만드는 데가 틀렸다** — 목이 가는 막대라 기린이 됐고, 머리가 공+주둥이 혹이라 개가 됐고, 갈기가 목 뒤 막대였다. 목은 어깨에서 넓은 아치로, 머리는 **하나로 이어진 긴 쐐기**로, 갈기는 목마루를 **타고 흐르는 면**으로. 몸통도 타원 하나 → 가슴·엉덩이가 부풀고 등이 파인 곡선. 앞머리·콧등 흰 줄·콧구멍·무릎 접힘 방향 추가 | src/ui/horse.ts |
| 08-07 | **`HORSE_BOX` 를 내보내 배치 검사가 그림 크기를 읽게 했다** | 배치 검사가 글자와 카드만 보고 **그림은 눈으로 어림했다.** 그래서 이름 화면에서 귀가 부제를 뚫었고 마방에서 꼬리가 카드 테두리에 닿았다. 그림을 바꾸면 검사가 깨진다 | src/ui/horse.ts, src/layout.test.ts |
| 08-07 | (버그) 헤더에서 **폰트를 바꾼 뒤에 이름 폭을 쟀다** | `measureText` 가 작은 mono 로 이름을 재서 폭이 모자랐고, 긴 이름에서 `번개블레이즈1800m` 처럼 붙었다. 재고 나서 바꾸는 순서로 | src/scenes/game.ts |
| 08-07 | 트랙 — 말 이름을 **레인 왼쪽 위 고정**으로, 잔디 아래끝을 안내줄 위로 | 이름이 말 옆(`x-40`)이라 출발선에서 화면 밖으로 잘리고 달리는 내내 말 그림에 깔렸다. 잔디는 `H-34` 라 마지막 레인이 안내줄에 2px 까지 붙었다 | src/scenes/game.ts |
| 08-07 | **개발 전용 `?go=<phase>` 진입을 넣었다** (`import.meta.env.DEV` 안, 빌드에서 빠진다) | 캔버스 게임이라 화면을 헤드리스로 볼 수가 없어서 **배치 겹침을 매번 사람이 발견했다.** 이제 `chrome --headless --screenshot` 으로 페이즈별로 찍어서 내가 직접 본다. 좌표 계산만으로는 이번 겹침 넷 중 셋을 못 잡았다 | src/main.ts |
| 08-07 | **트랙만 CC0 스프라이트로, 나머지는 벡터 유지** (사용자 판정) | 갤럽은 실제 프레임 5장이 손으로 그린 사인파 다리보다 압도적으로 낫고, 트랙에서는 말이 작아(높이 48px) 픽셀이 안 도드라진다. 반대로 이름·마방·결산은 말이 크게 보이고 **표정(웃음·눈물)이 필요한데 스프라이트에는 없다.** 값은 **같은 말이 화면마다 다르게 생기는 것** — 사용자가 알고 택했다 | src/ui/sprite.ts, docs/CREDITS.md |
| 08-07 | **색조 각도를 손으로 안 적고 팔레트에서 계산한다** | 스프라이트는 갈색 한 벌뿐이라 6두를 만들려면 색을 돌려야 한다. 각도 6개를 상수로 박으면 팔레트를 바꿀 때 안 따라온다 — **이 저장소가 손으로 고른 상수로 다섯 번 뎄다.** `hsl(coat.body)` 와 시트 기준색(#AF5E2D, 실측 최빈색)의 차를 런타임에 낸다 | src/ui/tokens.ts `hsl`, src/ui/sprite.ts `tint` |
| 08-07 | 스프레이트를 못 받으면 **벡터로 돌아간다** | 그림 하나 때문에 게임이 멈추면 안 된다. 배치 검사도 **두 경로를 다 잰다** — 스프라이트만 맞췄다가 대체 경로가 겹치면 오프라인에서만 깨진다 | src/scenes/game.ts, src/layout.test.ts |
| 08-07 | 트랙 착순 배지를 **레인 오른쪽 고정**으로 | 말 머리 위에 붙였는데 스프라이트가 벡터보다 납작(30단위 vs 88단위)해서 배지가 윗 레인으로 넘어갔다 | src/scenes/game.ts |
| 08-07 | 개발용 `?t=` 로 **경주 시각을 고정**할 수 있게 했다 | `--virtual-time-budget` 이 시계를 빨리 감아서 헤드리스 스크린샷이 매번 정산 화면을 찍었다. 달리는 중을 못 보면 트랙 배치를 못 고친다 | src/main.ts |
| 08-07 | **트랙 스프라이트를 되돌렸다** — 다시 벡터 말 (사용자 판정) | 실제로 화면에서 보고 아니라고 했다. 코드·그림·`docs/CREDITS.md` 를 **지웠다** — 안 쓰는 렌더러를 남기면 다음 사람이 "이건 왜 있나"를 묻게 된다. 되살리려면 `9db4f41` 에 그대로 있다 | src/ui/sprite.ts(삭제), public/(삭제) |
| 08-07 | (버그) **평판 카드가 `단승·연승·예상` 머리글을 6px 덮었다** | 카드 아래끝 120, 머리글 글자 윗선 114.4. **배치 검사가 카드는 재면서 바로 밑에 앉는 머리글은 목록에 없었다** — 그래서 통과했다. 머리글과 `승식` 라벨을 목록에 넣고, 평판 카드를 82~114 로 올리고 배당표·승식 열을 8px 내렸다 | src/scenes/game.ts, src/layout.test.ts |
| 08-07 | **M4 닫음** — HT-007 DONE. 사람 판정 7문항 중 5개 회신, **4번(유리 대비)·7번(받아들인 넷)은 M5 로 이월** | 둘 다 규칙이 아니라 판정이라 출하 단계에서 화면과 함께 물으면 된다. 답을 지어내는 것보다 열어둔 채 넘기는 것이 정직하다 | tasks/HT-007.md, records/HT-007.json |
| 08-07 | **M5 시작** — `npm run evidence` 한 명령으로 빌드·검사·실측·화면·라이선스·AI기록 | M1~M4 의 밸런스 수치가 전부 `/tmp` 의 일회용 스크립트에서 나왔다. **문서에 숫자만 남고 만드는 법이 없었다** — 값을 하나 고치면 문서 전체가 조용히 거짓이 되는데 아무도 모른다 | scripts/evidence.mjs |
| 08-07 | **참조 플레이어를 검사 파일에서 꺼냈다** (`session.test.ts` → `systems/policy.ts`) | `playSession` 이 검사 안에 있어서 vitest 밖에서 못 불렀다. 그게 실측이 매번 일회용 스크립트가 된 이유다. 이제 **검사와 증거가 같은 모형**을 쓴다. 옮긴 뒤 번들 해시가 그대로라(`index-B4WUz145.js`) 게임 코드는 안 바뀐 것이 확인된다 | src/systems/policy.ts |
| 08-07 | 증거 실행은 **실패하면 멈춘다** | 반쯤 채워진 `evidence/` 가 없는 것보다 나쁘다 — 재현됐다고 읽힌다. `REPORT.md` 는 언제나 쓰이고 안 돈 단계와 "나머지 파일은 이전 실행 것일 수 있다"가 적힌다. 일부러 실패시켜 확인했다 | scripts/evidence.mjs |
| 08-07 | 실측표에 **문서가 주장하는 값을 같이 적고 어긋나면 ⚠** | 검사는 "깨지면 안 되는 성질"(인기마 25~45%)을 지키고, 실측은 "지금 실제로 얼마인가"를 본다. 34%→31% 로 움직여도 검사는 안 깨지지만 문서는 틀린 값을 말하게 된다. **어긋나면 고칠 곳은 코드가 아니라 문서다** | src/evidence/measure.ts |
| 08-07 | `ai-use-log.yaml` 을 run record 에서 **생성**한다 | 손으로 쓰면 다음 작업에서 안 따라오고 제출 문서가 조용히 거짓이 된다. 파일 머리에 "생성물이다. 직접 고치지 마라"를 적었다 | scripts/ai-use-log.mjs |
| 08-07 | `nan/trace.yaml` 이 **실제 파일을 가리킨다.** 사슬을 셋으로 나눴다 | 자리표시자 한 줄(THEME-001 → … → EVD-001)이라 통과는 하는데 아무것도 안 말했다. 이 게임의 주장이 셋(정보 비대칭 · 비결정론 · 곡선)이라 사슬도 셋이다 | nan/trace.yaml |
| 08-07 | **받아들이기로 한 넷을 사람이 확정했다** — 조합 승식 정보우위 희석 · 강화 확률표 27개 값 · 공제율 20% · 상금 신설 | M4 에서 "안 고치고 출하한다"로 분류만 해두고 동의를 못 받아 열려 있었다. **넷 다 이대로 간다** — 이제 미해결이 아니라 **설계 결정**이다 | nan/gates.yaml |
| 08-07 | **유리 카드 대비는 그대로 둔다** (사람 판정) | 창구 화면을 보고 "누를 수 있는 것과 정보인 것이 구분된다"고 판정했다. **손대지 않는 것도 결정이다** — 안 물었으면 근거 없이 대비를 올렸을 것이다 | — |
| 08-07 | **공개 전환 + GitHub Pages 배포** (사람 판정) | 비공개 Pages 는 유료 플랜이 필요하다. 공개 전 히스토리 전체에서 키·토큰을 훑어 없는 것을 확인했다. 배포는 **검사를 통과한 것만** 올린다 — 빌드만 돌리면 깨진 게임이 올라간다 | .github/workflows/pages.yml |
| 08-07 | (버그) **로컬 빌드가 홈 폴더의 `~/node_modules/@types/node` 를 주워 쓰고 있었다** | TypeScript 는 `@types` 를 찾을 때 상위 디렉터리로 계속 올라간다. 프로젝트에 `@types/node` 가 없는데 `npm run build` 는 초록불이었고 **CI 첫 실행에서 깨졌다** — `measure.ts` 의 `node:fs`·`process` 를 못 찾았다. **증거 보고서가 이걸 통과로 적고 있었다.** `@types/node` 를 devDependency 로 선언하고 `tsconfig.types` 에 `node` 를 넣었다 | package.json, tsconfig.json |
| 08-07 | **`npm run isolated`** — tsc 가 읽은 파일이 전부 프로젝트 안인지 검사 | 위 버그를 다시 놓치지 않으려면 "빌드가 통과했다"만으로 부족하다. **어디 것을 읽고 통과했는지**를 봐야 한다. 증거 실행과 CI 양쪽에 넣었다 | scripts/isolated.mjs |
| 08-07 | **`honest-length` 를 닫았다가 다시 열었다** — 12경주 완주가 **5분**이다 | 목표 10~20분의 절반. M4·M5 를 닫을 때 쓴 "약 10분" 은 **처음 플레이한 사람**의 시간이었고, 규칙을 아는 사람은 5분에 끝낸다. **그 차이를 안 묻고 하나를 실측으로 삼아 게이트를 닫았다.** 게다가 12R 고정이 판을 20% 늘린다고 추정해 "12분 안팎"이라 문서에 적었는데 방향이 반대였다 | nan/gates.yaml |
| 08-07 | **경주 12 → 20** (사용자 판정) | 실측 분해 — 강제 대기(연출) 78s 26% · 판단·조작 222s 74% = **경주당 약 25초**. 길이는 곱셈이고 동결 안에서 정직한 레버는 경주 수뿐이다. **연출을 늘려 채우지 않는다** — 10분을 만들려면 대기를 5배로 해야 하고 그건 `length-from-state-not-content` 가 막는 부풀리기다 | src/systems/scale.ts |
| 08-07 | **규모를 `src/systems/scale.ts` 한 곳으로** — 압박 곡선을 기울기가 아니라 **끝점**으로 적는다 | 12→20 을 그냥 바꾸니 파산 11%→**39%**, 참여율 95%→**68%** 로 무너졌다. 원인은 `base = 3 + (no-1) × 0.32` — **경주 번호에 비례**하니 판이 길어지면 상대가 천장(10)까지 올라간다. `RIVAL_GROWTH` 를 0.185 로 고쳐도 되지만 **그게 손으로 고른 상수로 데는 여섯 번째**다. 끝점(마지막 상대 6.5)으로 적으면 경주 수를 바꿔도 곡선 모양이 같다 — 고친 뒤 파산 15% · 참여율 91% · 인기마 38% | src/systems/scale.ts, src/systems/race.ts |
| 08-07 | **검사도 경주 번호를 박아뒀다** — 국면을 비율로 바꿨다 | `11R = 후반` 이 20경주에서는 중반이 되어 curve 검사 3개가 깨졌다. `EARLY/MID/LATE` 를 `RACES` 에서 파생시킨다. balance 의 "강화 안 하면 우위 없다"도 `5경주·5단계` 를 박아뒀는데, **"상대 평균이 그 수준인 경주"를 찾는 방식**으로 바꿨다 — 경주 번호가 아니라 뜻을 적는다 | src/curve.test.ts, src/balance.test.ts |
| 08-07 | (발견) **최약체 승률 0% → 3%.** M4 가 "순수 함정"으로 기록한 것이 절로 풀렸다 | 20경주로 늘리며 압박 곡선이 완만해져 출주표가 촘촘해졌다. 실측이 ⚠ 로 잡아줬다 — **이게 `measure.ts` 를 만든 이유다** | src/evidence/measure.ts |
| 08-07 | (발견) **안정 스탯 승률 −1.8%p → +2.2%p.** 부호가 뒤집혔다 | 버그가 아니다. 20경주에서 같은 지점의 상대가 약해져 기준 말이 상대적으로 강해졌고, **강한 말에게 안정은 지키는 값**이 된다 — HT-006 이 "국면에 따라 값이 뒤집히는 유일한 스탯"이라고 설계한 그대로 움직였다 | docs/GAME_SPEC.md |
| 08-07 | (버그) **한글 이름이 낱자로 들어갔다** — "가나" 가 `ㄱㅏㄴㅏ` | 캔버스가 `keydown` 을 직접 받아 `e.key` 를 이어붙였는데, **한글 `keydown` 은 조합 중인 낱자를 준다.** IME 조합은 브라우저만 할 수 있다. 화면 밖으로 안 치우고 **캔버스 위에 투명한 `<input>`** 을 겹쳐 결과 문자열만 받는다 — 밖으로 치우면 모바일에서 키보드가 화면을 스크롤시키고 포커스가 튄다 | src/main.ts, src/scenes/game.ts |
| 08-07 | 이름을 **한 글자씩 안 받는다** — `setName(전체)` · `submitName()` | `key(k)` 로 한 자씩 붙이는 구조 자체가 IME 와 맞지 않는다. 이름 화면에서 `key` 는 아무것도 안 하게 만들어 **낱자가 새어들 자리를 없앴다** | src/scenes/game.ts |
| 08-07 | 글자 수를 **코드포인트**로 센다 (`[...v].slice`) | `String.length` 는 이모지를 둘로 세서 `slice` 하면 반쪽이 남는다. `🐎` 20개를 넣어 확인했다 | src/scenes/game.ts |
| 08-07 | **조합 중 Enter 는 확정이지 제출이 아니다** (`e.isComposing`) | 안 막으면 "가" 를 확정하려고 누른 Enter 가 판을 시작해버린다 | src/main.ts |
| 08-07 | **`honest-length` 를 닫았다** — 20경주를 완주하고 "적당하다"고 판정 | **측정**: 같은 사람이 12경주를 5분에 끝냈고(경주당 25초) 20경주를 완주해 길이를 받아들였다. **파생**: 20경주 ≈ 8분. 지난번 오판(처음 하는 사람 10분 ↔ 아는 사람 5분)과 달리 **같은 사람·같은 숙련도**의 값을 늘린 것이라 근거가 다르다 | nan/gates.yaml |
| 08-07 | **원래 목표 10~20분을 `acceptedLength` 로 대체했다** — 20경주 · 약 8분 | 8분은 목표에 못 미친다. 하지만 그 10~20분은 **플레이 전에 고른 숫자**이고, 실제로 해보고 8분이 맞다고 한 판정이 그것을 이긴다. **길이를 숫자에 맞추려고 게임을 늘리지 않는다** — 늘릴 수 있는 정직한 레버는 경주 수뿐이고, 그건 이미 사람이 20으로 정했다 | nan/gates.yaml, docs/GAME_SPEC.md |
| 08-07 | (버그) **증거 실행이 가끔 실패했다** — 화면 단계가 고정 포트 4271 에 `--strictPort` | 이전 실행의 vite 가 안 죽었으면 즉시 실패하고, 다음 번엔 통과했다. **가끔 실패하는 증거는 증거가 아니다.** vite 가 빈 포트를 고르게 두고 실제로 뜬 주소를 stdout 에서 읽는다. 연속 3회 통과 확인 | scripts/shots.mjs |

